import { API_TIMEOUT, API_URL } from '../config/api';

/**
 * Default coordinates for Mati City (Davao Oriental, Philippines).
 */
export const EXPLORE_DEFAULT_COORDS = { lat: 6.9510513, lng: 126.2195947 };

/** @deprecated Use API_URL from config/api — kept for existing imports */
export const API_BASE_URL = API_URL;

export { API_TIMEOUT, API_URL };
